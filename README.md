
  # Design UI with Image Context (Copy) (Copy)

  This is a code bundle for Design UI with Image Context (Copy) (Copy). The original project is available at https://www.figma.com/design/gRMUAAnxXVXKLbU6jO6jEn/Design-UI-with-Image-Context--Copy---Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  